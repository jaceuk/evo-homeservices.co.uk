---
import Section from '../Section.astro';
import NavList from '../header/NavList.astro';
import NavListItem from '../header/NavListItem.astro';
import Legal from './Legal.astro';

type Props = Record<string, never>;
---

<Section>
	<h2 class="sr-only">Links</h2>
	<div class="wrapper">
		<nav>
			<h3 class="h4 coloured-text">EVO Drainage Services</h3>
			<NavList>
				<NavListItem href="/" text="Home" />
				<NavListItem href="/about" text="About" />
				<NavListItem href="/recruitment" text="Recruitment" />
				<NavListItem href="/contact" text="Contact" />
			</NavList>
		</nav>

		<nav>
			<NavList>
				<NavListItem href="/blocked-drains" text="Blocked Drains" />
				<NavListItem href="/cctv-drain-surveys" text="CCTV Drain Surveys" />
				<NavListItem href="/drain-repairs" text="Drain Repairs" />
				<NavListItem href="/soakaways-septic-tanks" text="Soakaways and Septic Tanks" />
				<NavListItem href="/gutter-cleaning" text="Gutter Cleaning" />
			</NavList>
		</nav>
	</div>

	<p class="coverage">
		<small>
			We cover
			<a href="/services/east-sussex">East Sussex</a>,
			<a href="/services/hampshire">Hampshire</a>,
			<a href="/services/kent">Kent</a>,
			<a href="/services/london">London</a>,
			<a href="/services/surrey">Surrey</a>, and
			<a href="/services/west-sussex">West Sussex</a>.
		</small>
	</p>
</Section>

<style lang="scss">
	.wrapper {
		display: none;
		align-items: start;
		gap: var(--scale-32-64);

		@media (min-width: 768px) {
			display: flex;
		}
	}

	nav {
		flex: 1;
		display: grid;
		gap: var(--scale-8-16);
		border-right: 1px solid var(--c-grey);

		&:last-child {
			border: none;
		}
	}

	ul {
		display: grid;
		gap: var(--scale-8-16);
		padding: 0;
	}

	li {
		list-style: none;
	}

	a {
		font-weight: var(--fw-semi-bold);
	}

	.coverage {
		text-align: center;
		margin: auto;

		@media (min-width: 768px) {
			padding: var(--scale-16-32) 0 0 0;
		}
	}
</style>
